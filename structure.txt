url-shortener/
│
├── cmd/
│   └── web/
│       └── main.go                           ← entry point, wires all layers
│
├── internal/
│   ├── config/
│   │   └── config.go                         ← loads .env into Config struct
│   │
│   ├── errors/
│   │   └── errors.go                         ← ErrNotFound, ErrExpired, ErrInactive
│   │
│   ├── models/
│   │   ├── url.go                            ← URL, ShortCode, ClickEvent structs
│   │   └── analytics.go                      ← AnalyticsAggregate, DailyCount structs
│   │
│   ├── repository/
│   │   ├── repository.go                     ← URLStore, ClickStore, CacheStore interfaces
│   │   ├── url_repo.go                       ← PostgresStore — source of truth
│   │   ├── url_repo_test.go                  ← unit tests (sqlmock)
│   │   ├── cache_repo.go                     ← RedisCache — fast lookups
│   │   └── cache_repo_test.go                ← unit tests (mock redis)
│   │
│   ├── service/
│   │   ├── shortener.go                      ← Shorten, Resolve, Delete logic
│   │   ├── shortener_test.go                 ← unit tests (mock store + cache)
│   │   ├── analytics.go                      ← GetAnalytics, aggregation logic
│   │   └── analytics_test.go                 ← unit tests (mock clickstore)
│   │
│   ├── handler/
│   │   ├── handler.go                        ← Handler struct, NewHandler
│   │   ├── home.go                           ← GET /
│   │   ├── shorten.go                        ← POST /shorten
│   │   ├── shorten_test.go                   ← unit tests (mock service)
│   │   ├── redirect.go                       ← GET /:code → 301
│   │   ├── redirect_test.go                  ← unit tests (mock service)
│   │   ├── analytics.go                      ← GET /:code/stats (JSON API)
│   │   ├── analytics_test.go                 ← unit tests
│   │   ├── admin.go                          ← GET /admin/stats (HTML dashboard) ← NEW
│   │   ├── admin_test.go                     ← unit tests ← NEW
│   │   └── health.go                         ← GET /health → 200 OK
│   │
│   ├── middleware/
│   │   ├── ratelimit.go                      ← per-IP token bucket
│   │   ├── metrics.go                        ← capture + persist click events
│   │   ├── metrics_test.go                   ← unit tests (mock clickstore)
│   │   ├── auth.go                           ← API key validation
│   │   ├── cors.go                           ← CORS headers
│   │   └── timeout.go                        ← request timeout context
│   │
│   ├── routes/
│   │   └── routes.go                         ← all route + middleware registration
│   │
│   ├── helpers/
│   │   └── helpers.go                        ← RenderPage jet template helper
│   │
│   └── templates/
│       └── templates.go                      ← TemplateData struct
│
├── pkg/
│   ├── hash.go                               ← Base62/nanoid short code generation
│   ├── respond.go                            ← standardized JSON response helpers
│   ├── sanitize.go                           ← URL validation and sanitization
│   └── logger.go                             ← structured logger (zerolog/zap)
│
├── views/
│   ├── home.jet                              ← home page — shorten form
│   └── admin.jet                             ← admin dashboard — analytics ← NEW
│
├── static/
│   └── css/
│       ├── input.css
│       └── output.css                        ← compiled (gitignored)
│
├── migrations/
│   ├── 000001_create_urls_table.up.sql
│   ├── 000001_create_urls_table.down.sql
│   ├── 000002_create_clicks_table.up.sql
│   ├── 000002_create_clicks_table.down.sql
│   ├── 000003_create_api_keys_table.up.sql
│   └── 000003_create_api_keys_table.down.sql
│
├── docker/
│   ├── Dockerfile
│   └── Dockerfile.dev
│
├── .github/
│   └── workflows/
│       └── ci.yml
│
├── .env
├── .env.example
├── .gitignore
├── docker-compose.yml
├── docker-compose.dev.yml
├── Makefile
├── go.mod
├── go.sum
├── package.json
├── postcss.config.js
└── README.md